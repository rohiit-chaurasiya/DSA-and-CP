import java.util.*;
public class OlyaGamewithArrays_CF {
    public static void main(String[] arg) {
        Scanner in = new Scanner(System.in);
        int kiet = in.nextInt();
        if (kiet >= 1) {
            while (kiet-- > 0) {
                int len = in.nextInt();
                if (len >= 1)
                {
                    int lenm=in.nextInt();

                    for (int i = 0; i < len; i++) {
                        ArrayList<Integer> arr = new ArrayList<>();
                        arr.add(in.nextInt());
                    }
                }
            }
        }
    }


}
